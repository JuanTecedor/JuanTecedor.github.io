---
interface Props {
    title: string;
    tagline?: string;
}

const { tagline, title } = Astro.props;
---

<div>
    <div>
        <h1>{title}</h1>
        {tagline && <p class="tagline">{tagline}</p>}
    </div>
    <slot />
</div>
